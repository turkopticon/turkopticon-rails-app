%span.title= title_from_params params, @page[:target]
.heading-bar
  = page_entries_info @reviews, entry_name: 'review'
  = paginate @reviews, window: 2

= render 'header'

- @reviews.each do |r|
  = render 'row', r: r

= javascript_include_tag 'reviews/comments'
