%span.title Edit review

- t = 2.months.ago
- if @authorized && @review.created_at > t
  = render 'nested_form'
- elsif @review.created_at < t
  %p To preserve data integrity, this review has been archived and can no longer be edited.
- else
  %p You do not have permission to make edits on this review.
